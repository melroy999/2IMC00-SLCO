{% for node in model.decisions %}
{{ "else" if not loop.first }}if ({{ node | render_java_instruction }}) {
    {% if node | is_decision_node %}
    {{ node | render_decision_node | indent(4, False) | trim }}
    {% else %}
    currentState = {{ node.parent.name }}Thread.States.{{ node.target }};
    {% endif %}
}{{ "
" if loop.last }}
{%- endfor %}