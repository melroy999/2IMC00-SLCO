// Representation of SLCO transition {{ model.source }}_{{ model.id }} ({{ model.source }} -> {{ model.target }})
private boolean execute_transition_{{ model.source }}_{{ model.id }}() {
    {% for s in model.statements %}
    {{ s | render_statement | indent(4, False) | trim }}

    {% endfor %}
    return true;
}