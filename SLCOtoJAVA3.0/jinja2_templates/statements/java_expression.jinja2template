{% if not exclude_comment %}// SLCO statement: {{ model.get_original_statement() }} -> {{ model }}{% endif %}
if(!({{ in_line_expression }})) {
    {% if is_guard %}
    return false;
    {% else %}
    return true;
    {% endif %}
}