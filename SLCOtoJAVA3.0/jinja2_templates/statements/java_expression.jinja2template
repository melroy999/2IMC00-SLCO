{% if not exclude_comment %}
// SLCO expression !({{ model.get_original_statement() }}) -> !({{ model }}) {% if model.exclude_statement %}(Superfluous){% endif %}

{% endif %}
if(!({{ in_line_expression }})) {
    {% if is_guard %}
    return false;
    {% else %}
    return true;
    {% endif %}
}